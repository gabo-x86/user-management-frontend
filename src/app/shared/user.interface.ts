import { Time } from "@angular/common";

export interface User {
    id: number;
    username: string;
    password: string;
    email: string;
    createdAt: string;
    firstName: string;
    lastName: string;
    age: number;
    birthDay: string;
}